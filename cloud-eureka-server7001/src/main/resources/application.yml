server:
  port: 7001

eureka:
  instance:
    hostname: eureka.com:7001 #eureka服务端的实例名称
  client:
    #false表示不向注册中心注册自己
    register-with-eureka: false
    #false表示自己端就是注册中心
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka/